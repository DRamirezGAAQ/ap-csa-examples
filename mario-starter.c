//Feel free to use this code as a starting template to write mario.c
#include <stdio.h>
#include <cs50.h>

int main(){
    int height; //You will need to fill this with user input (ask them to enter something)
    int spaces; //This will change depending on the behavior in your for loops
    int hashes; //This will change depending on the behavior in your for loops

    for (int row = 0; row < height; row++){

        //You will need two loops nested inside of this main loop for your program to work
        //There are multiple ways to solve this problem, using two nested loops is only one of them!
        //Good Luck!
}
